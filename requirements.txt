# Core dependencies
streamlit
chromadb>=0.4.24  # Make sure you're using a Chroma version that needs sqlite3 >= 3.35.0
sentence-transformers
requests
python-dotenv

# Used to compare version of sqlite
packaging

# ⚠️ NOTE: sqlite3 is part of Python's standard library.
# You must ensure system sqlite3 version is >= 3.35.0 manually.
# See: https://docs.trychroma.com/troubleshooting#sqlite
